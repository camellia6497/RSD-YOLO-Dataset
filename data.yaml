# Please insure that your custom_dataset are put in same parent dir with YOLOv6_DIR
train: /root/later-data/train/images # train images
val: /root/later-data/val/images # val images
test: /root/later-data/test/images # test images (optional)

# whether it is coco dataset, only coco dataset should be set to True.
is_coco: False
# Classes
nc: 3  # number of classes
names: ['spalling','wheel burn','squat']  # class names